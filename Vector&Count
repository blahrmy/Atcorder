#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;

  vector<int> vec(N); // N個のマス

  // データを受け取る
  for (int i = 0; i < N; i++) {
    cin >> vec.at(i);
  }



  // 合計点を出力
  for (int i = 0; i < N; i++) {
    cout << math.at(i) + english.at(i) << endl;
  }
}
